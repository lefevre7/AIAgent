export default {
  // Matches the width the codebase is actually written at. The default (80)
  // would reflow nearly every file, so `npm run format` was a landmine.
  printWidth: 120,
  semi: true,
  singleQuote: false,
  trailingComma: "none"
};
